import options from '../../../utils/options';
import { module, test } from 'qunit';

module('Unit | Utility | options');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = options();
  assert.ok(result);
});
